<%= form_for(@host, :html => {:class => 'form-horizontal'}) do |f| %>
  <% if @host.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@host.errors.count, "error") %> prohibited this host from being saved:</h2>

      <ul>
      <% @host.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="span6">
      <div class="control-group">
        <%= f.label :name %>
        <div class="controls">
          <%= f.text_field :name %>
        </div>
      </div>

      <div class="control-group">
        <%= f.label :email %>
        <div class="controls">
          <%= f.text_field :email %>
        </div>
      </div>

      <div class="control-group">
        <%= f.label :phone %>
        <div class="controls">
          <%= f.text_field :phone %>
        </div>
      </div>

      <div class="control-group">
        <%= f.label :twitter %>
        <div class="controls">
          <div class="input-prepend">
            <span class="add-on">@</span>
            <%= f.text_field :twitter, :class => 'span2' %>
          </div>
        </div>
      </div>

      <div class="control-group">
        <%= f.label :address1, 'Address' %>
        <div class="controls">
          <%= f.text_field :address1 %><br />
          <%= f.text_field :address2 %>
        </div>
      </div>

      <div class="control-group">
        <%= f.label :zip %>
        <div class="controls">
          <%= f.text_field :zip %>
        </div>
      </div>

      <div class="control-group">
        <%= f.label :number_of_guests, 'Number of guests you can host' %>
        <div class="controls">
          <%= f.select :number_of_guests, (1..10) %>
        </div>
      </div>

      <div class="control-group">
        <%= f.label :description %>
        <div class="controls">
          <%= f.text_area :description, :rows => 5 %>
          <p class="help-block">Tell us a little bit about your place.</p>
        </div>
      </div>

      <div class="form-actions">
        <%= f.submit 'Submit', :class => 'btn btn-primary' %>
      </div>
    </div>

    <div class="span6">
      <p>Think of it has AirB&B except you'll actually have something in common.</p>
      <p>When we find guests that might be a good match, we'll contact you with their details and give you a chance to vet before confirming anything.</p>
    </div>
  </div>

  
<% end %>
