<div class="details">
    <h1>Ignite RailsConf</h1>
    <h2>April 22nd 6:00pm - 9:00pm</h2>
    <h2>Hilton Austin, Salon J</h2>

    <b>Five minutes, 20 slides. What would you say?</b> Sixteen artists, technologists, thinkers, and personalities take the stage to answer this challenge.

    <p>Follow <%= link_to '@igniterailsconf', 'http://twitter.com/igniterailsconf' %> to keep up with the latest news on Ignite RailsConf.</p>

  <p><%= link_to 'Submit your idea for a talk', new_speaker_path, :class => 'btn btn-large btn-primary' %></p>

  <% @ignite_speakers = Speaker.where(:hide => false) %>
  <% if @ignite_speakers.any? %>
  <h3>Proposed Talks</h3>
	<ul>
		<% Speaker.where(:hide => false).each do |speaker| %>
			<li>
				<h4>
					<% if speaker.twitter.present? %>
						<%= image_tag("http://api.twitter.com/1/users/profile_image/#{speaker.twitter}?size=normal") %>
					<% end %>
					<%= link_to_if speaker.twitter.present?, speaker.name, "http://twitter.com/#{speaker.twitter}" %>:
					<%= speaker.title %>
				</h4>
				<p class="normal">
					<%= speaker.abstract %>
				</p>
			</li>
		<% end %>
	</ul>
  <% end %>
</div>